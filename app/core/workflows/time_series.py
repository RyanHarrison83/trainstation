import streamlit as st

def run(dataset_path, goal=None):
    """Placeholder time series forecasting workflow.

    Parameters
    ----------
    dataset_path : str
        Path to the dataset directory.
    goal : str, optional
        Optimization goal selected by the user. Currently unused.
    """

    st.success("📈 Time Series workflow started!")
    st.write("Darts or Prophet could be used here.")
