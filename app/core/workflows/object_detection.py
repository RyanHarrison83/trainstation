import streamlit as st

def run(dataset_path, goal=None):
    """Placeholder object detection workflow.

    Parameters
    ----------
    dataset_path : str
        Path to the dataset directory.
    goal : str, optional
        Optimization goal selected by the user. Currently unused.
    """

    st.success("🧪 Object Detection workflow started!")
    st.write("This is where YOLOv8 or similar logic will go.")
